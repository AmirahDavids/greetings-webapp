language: node_js
env:
  - DB=postgresql://postgres@localhost:5432/greeter

node_js:
  - 12

before_script: 
  # - sudo sed -i -e '/local.*peer/s/postgres/all/' -e 's/peer\|md5/trust/g' /etc/postgresql/*/main/pg_hba.conf
  # - sudo service postgresql restart
  - psql -c 'create database greeter;' -U postgres
  - psql -U postgres -d greeter -a -f database.sql

addons:
  apt:
  packages:
    - postgresql-server-dev-9.2